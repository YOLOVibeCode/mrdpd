# M6 tasks — inject input (CGEvent behind InputSink)

Survival gate: live desktop **and** injected keyboard/mouse. Do not start T2 (clipboard, resize, multimon, EGFX, audio). No new C ABI. `InputSink` still has only `handle(_:)`.

Depends on M5: `just serve` live view. iPad still deferred until a device exists (`docs/tasks/m2.md` / `m5.md`).

## Mapping (T1-IN-03 Retina)

1. [x] `DisplayMap`: RDP virtual-desktop pixels → Quartz global points (scale + origin). Not an `InputSink` method (ISP).

## CGEventInputSink (T1-IN-04)

2. [x] `InjectionPlan` turns `InputEvent` into postable key/mouse/wheel (TCC-free).
3. [x] `CGEventInputSink` conforms to `InputSink` (`handle` only) and posts HID events.
4. [x] `just test` stays TCC-free: CGEvent post tests skip unless `MRDPD_TEST_LOCAL=1`.
5. [x] `just test-local` requires Accessibility TCC and fails if it is missing.

## App wiring

6. [x] `mrdpd-serve` uses `CGEventInputSink` (not `RecordingInputSink`). Missing Accessibility exits with `docs/tcc.md`.
7. [x] T1-PERF-02 noted in `docs/bench.md` (not CI).

## Interop (`docs/interop.md`)

8. [x] Local HID mouse (`just test-local`). FastPath mouse against live `mrdpd-serve` (`just test-inject`). iPad typing deferred — no device.

## Out of scope

- Unicode IME (T2-IN-01), horizontal wheel / XButtons (T2-IN-02), RDPEI (T2-IN-03)
- Clipboard (M7)
- New ABI symbols

## Notes (TDD + ISP)

- `DisplayMap.cgLocation` went red on 2× pixels and origin, then green as scale + clamp.
- `InjectionPlan` went red on empty plans, then green for A, Cmd+A flags, left click, drag, wheel 120.
- `InputSink` still has only `handle(_:)`. Key posting is not in `just test` (would type into the focused app).
- Local mouse move restores the cursor afterward.

## Interop log

- Date: 2026-08-21
- Client + version: IronRDP headless FastPath mouse (`tests/serve_inject.rs`). **N/A:** iPad Windows App (no device).
- Host macOS: darwin 25.3.0
- Result: **pass** (cursor moved from other display `-2172,1109` to captured-desktop center `1680,945` via `mrdpd-serve` on `127.0.0.1:3390` 3360×1890). **deferred:** iPad typing/clicking — no device.
- Evidence: `t1_in_04_fastpath_mouse_against_live_serve` ok in 2.79s; Quartz location `1680,945` after MOVE to (width/2, height/2). Cursor restored afterward.
- Follow-up IDs: C-IPAD M6 survival gate; T1-PERF-02; windowed FreeRDP typing if desired

## Interop log (C-IPAD)

- Date: 2026-08-21
- Client + version: Windows App iPadOS (user session; Tailscale `100.109.3.105:3390`)
- Host macOS: darwin 25.3.0; capture 3360×1890 (first `SCDisplay`)
- Result: **partial pass.** Desktop **showed and rendered**. Typing **some** keys worked. Session **not smooth** (RemoteFX at native panel pixels over Tailscale). Resize / Mac display-mode change **not in M6** (T1-MON-01 is M8). Click/drag/scroll and Cmd-shortcuts not fully scored.
- Evidence: user report; no black screen / NLA failure reported.
- Follow-up IDs: T1-MON-01 (RDPEDISP + SCK reconfigure, do **not** CGDisplaySetDisplayMode unless we ADR it); T1-PERF-01/02 (3360×1890 RemoteFX); T2-GFX-02 EGFX M10; T1-IN-03 first-display-only
