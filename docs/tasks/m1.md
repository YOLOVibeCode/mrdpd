# M1 tasks — real engine + E2E + spikes

Do not start until M0b `just test` is green and M0 DoD extra boxes are checked.

## Engine

1. Red: existing ABI suite still the source of truth; add a `just test` target that loads `mrdpd-engine` **instead of** StubEngine and expects identical results (minus stub-only test header).
2. Implement `mrdpd-engine` with IronRDP: TLS listen (T1-SEC-01), NLA with config creds (T1-SEC-02), accept one client (T1-SEC-03).
3. `push_frame` of a solid color is encoded so a headless client can screenshot it (T1-GFX-01, T1-GFX-02 as far as “something decodes”).
4. Poison-after-return still passes on the real dylib (R6).

## E2E harness

5. Headless IronRDP client connects, dumps BMP.
6. Golden: solid color (e.g. #FF00FF) pixel-exact or documented tolerance (T1-GFX-01).
7. Bind 127.0.0.1 only in tests (T1-SEC-04).

## Spikes (DoD — written findings required)

8. `docs/spikes/` R1 static multimon / GCC monitors in IronRDP server.
9. R2 MS-RDPEI server support.
10. R5 reconnect cookies.
11. Update `docs/risks.md` and any spec rows (T2-MON-01, T2-IN-03, T2-SEC-01) with the answer: implement / upstream PR / OUT.

## Traceability

12. Move T1-SEC-01, T1-SEC-02, T1-GFX-01 to at least `e2e-green` for C-HEADLESS.

## Interop

Optional FreeRDP connect to solid color. iPad can wait for M2 but a handshake smoke is welcome.

## Interop log

- Date:
- Client + version:
- Result:
- Evidence:
