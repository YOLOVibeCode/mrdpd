# M4 tasks — ScreenCaptureKit behind FrameSource

Do not start T2. M6 is still the survival gate. No `CGEventInputSink`, clipboard, audio, or AVC.

Depends on M3: keymap + FastPath sequence green. iPad pattern still deferred (`docs/tasks/m2.md`).

## Dirty rects (T1-GFX-01 / start of T1-GFX-04)

1. [x] Pure-data clip + empty-list-means-full-frame (ABI v1). Not a `FrameSource` method (ISP).
2. [x] Copy BGRA from a `CVPixelBuffer` with real stride (TCC-free).

## SCKFrameSource (same contract)

3. [x] `SCKFrameSource` conforms to `FrameSource` (`nextFrame()` only).
4. [x] Same `testFrameSourceContract(_:)` as `SyntheticFrameSource`.
5. [x] `just test` stays TCC-free: SCK tests skip unless `MRDPD_TEST_LOCAL=1`.
6. [x] `just test-local` requires Screen Recording TCC and fails if it is missing.

## Ops (T1-OPS-03)

7. [x] Document Screen Recording TCC (how to grant; CI never grants). Accessibility waits for M6.

## Out of scope this milestone

- Cursor composite (T1-GFX-05, M5)
- Frame pacer / 60 fps cap (T1-GFX-04 rest, M5)
- Live LAN view (M5)
- New C ABI symbols

## Notes (TDD + ISP)

- `DirtyRects.normalized` went red on `[]`, then green as clip + empty→full frame. `FrameSource` still has only `nextFrame()`.
- `CaptureFrame.make` went red on `nil`, then green as BGRA copy honoring `CVPixelBuffer` stride.
- `SCKFrameSource` went red on `notImplemented`, then green on Screen Recording TCC (`just test-local`).
- Cursor composite and frame pacing stay M5. No new C ABI.

## Interop log

- Date: 2026-08-20
- Client + version: n/a (capture constituent; no RDP client this milestone)
- Host macOS: darwin 25.3.0
- Result: pass (TCC-free DirtyRects + CaptureFrame). pass (`just test-local` SCK contract). **N/A:** iPad live desktop (M5).
- Evidence: `SCKFrameSource` yields BGRA `Frame` with dirty list; same `testFrameSourceContract` as synthetic.
- Follow-up IDs: T1-GFX-04 pacer M5; T1-GFX-05 cursor M5; T1-OPS-03 Accessibility M6.

