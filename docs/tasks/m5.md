# M5 tasks — live desktop view (SCK + real engine)

Do not start T2. M6 is still the survival gate. No `CGEventInputSink`, clipboard, audio, or AVC. No new C ABI.

Depends on M4: `SCKFrameSource` contract-green locally. iPad pattern still deferred (`docs/tasks/m2.md`).

## Pacer + cursor (T1-GFX-04 / T1-GFX-05)

1. [x] `FramePacer` caps emit at 60 fps. Not a `FrameSource` method (ISP).
2. [x] SCK composites the cursor into frames (`showsCursor` default true).

## Pump (T1-GFX-01 live path)

3. [x] `FramePump` pulls `FrameSource`, respects pacer, `push`es ABI v1. TCC-free with `SyntheticFrameSource`.
4. [x] Bind policy: refuse `0.0.0.0` / `::` / `*` (T1-SEC-04) in the Swift lab process.

## Lab process

5. [x] `mrdpd-serve`: SCK → pacer → real engine. Default `127.0.0.1:3390`. `just serve`.
6. [x] `just bench`: T1-PERF-01 synthetic 1080p ≥ 30 fps under the pacer. T1-PERF-03/04 noted for interop (not CI).

## Interop (`docs/interop.md`)

7. [x] FreeRDP: live desktop, view only (GDI BGRA32). iPad deferred — no device (same reason as `docs/tasks/m2.md`).

## Out of scope

- Injected input (M6)
- Resize / RDPEDISP (M8)
- New ABI symbols

## Notes (TDD + ISP)

- `FramePacer.shouldEmit` went red on a second emit inside 1/60 s, then green. `FrameSource` still has only `nextFrame()`.
- `SCKSettings().showsCursor` went red on `false`, then green as default `true`.
- `FramePump` always-push went red on the cap, then green with `FramePacer`.
- `BindHost` went red on `0.0.0.0`, then green. `mrdpd-serve` exits 4 before capture.
- `ServeArgs` went red when `swift run --` was treated as the host, then green by stripping `--`.

## Interop log

- Date: 2026-08-21
- Client + version: FreeRDP 3.30 `sdl-freerdp` (GDI sw). **N/A:** iPad Windows App (no device).
- Host macOS: darwin 25.3.0
- Result: **pass** (FreeRDP GDI `PIXEL_FORMAT_BGRA32` against live SCK at 3360×1890 on `127.0.0.1:3391`). **deferred:** iPad live view — no device. **not measured:** T1-PERF-03/04 idle kbit/s and CPU (session was a handshake, not an idle sit).
- Evidence: `mrdpd-serve listening 127.0.0.1:3391 3360x1890`; client log `gdi_init_ex: Local framebuffer format PIXEL_FORMAT_BGRA32`. FPS not instrumented beyond the 60 fps pacer cap.
- Follow-up IDs: T1-PERF-03/04 live numbers; C-IPAD live view; M6 injection
