# M3 tasks — input decode (keymap + ABI callbacks)

Do not start T2 feature work. M6 is still the survival gate. No `CGEventInputSink` (that is M6 / T1-IN-04).

Depends on M2: 1080p pattern E2E + FreeRDP GDI green. iPad pattern remains deferred (no device); named in `docs/tasks/m2.md`.

## Keymap (T1-IN-02)

1. [x] Pure-data US table: RDP Set-1 scancode + extended flag → macOS virtual keycode (`Events.h` `kVK_*`).
2. [x] `InputSink` stays `handle(_:)` only (ISP). Keymap is not a protocol method.
3. [x] Unknown scancodes return `nil`. Other layouts wait.

## ABI sequence (T1-IN-01 / T1-IN-03 mouse without Retina)

4. [x] Headless IronRDP client sends FastPath: mouse move, left click, vertical wheel, key down/up (scancode `0x1E` A).
5. [x] Engine `on_mouse` / `on_key` fire with virtual-desktop pixels and Set-1 scancodes (no new C ABI).
6. [x] Same sequence hops into `RecordingInputSink` (StubEngine scripted keys + EngineKit). No CGEvent.

## Traceability

7. [x] T1-IN-01 → at least `e2e-green` on C-HEADLESS.
8. [x] T1-IN-02 → `contract-green`.
9. [x] T1-IN-03 Retina / origin mapping stays M6; M3 proves FastPath mouse + wheel in virtual-desktop space only.

## Interop

No iPad/FreeRDP typing gate this milestone (`docs/interop.md` typing is M6). Headless FastPath is the E2E.

## Notes (TDD + ISP)

- Do not add clipboard, unicode IME, horizontal wheel, or XButtons (T2).
- Do not add production ABI symbols. Stub-only `mrdpd_stub_script_key` is allowed, same class as `mrdpd_stub_script_mouse`.
- Keymap test went red on `UsKeymap` returning `nil`, then green as Set-1 → `kVK_*`. `InputSink` still has only `handle`.
- EngineKit key hop went red on missing `mrdpd_stub_script_key`, then green on StubEngine. Real dylib does not export the symbol (`--allow-missing-stub-hooks`).
- FastPath E2E: `tests/input_sequence.rs` (A 0x1E down/up, move 100,200, left, wheel 120).

## Interop log

- Date: 2026-08-20
- Client + version: IronRDP headless 0.17 FastPath (`input_sequence.rs`)
- Host macOS: darwin 25.3.0
- Result: pass (C-HEADLESS). **N/A:** iPad typing (M6). **N/A:** FreeRDP typing (M6).
- Evidence: ABI `on_key` 0x1E down+up; `on_mouse` move (100,200), left button, wheel 120. StubEngine key sequence in `RecordingInputSink`.
- Follow-up IDs: T1-IN-04 CGEvent (M6); T1-IN-03 Retina mapping (M6).
