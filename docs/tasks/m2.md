# M2 tasks — 1080p test pattern through the real engine

Do not start T2 feature work. M6 is still the survival gate.

Depends on M1: `just test` green, `mrdpd-engine` ABI + solid-color E2E.

## Pattern

1. [x] Drive EngineKit with `SyntheticFrameSource` at 1080p (gradient or labeled pattern), not a 64×64 solid.
2. [x] Golden: headless IronRDP client BMP matches the pattern (document tolerance if RemoteFX is lossy at 1080p).
3. [x] Keep bind `127.0.0.1` only in automated tests.

## Codec (T1-GFX-02 / start of T1-GFX-03)

4. [x] Confirm what the wire actually sends (RemoteFX vs RDP 6 bitmap / RLE) and record it in the interop log.
5. [x] Do not add EGFX / AVC ABI (ISP, M10).

## Interop (required this milestone per `docs/interop.md`)

6. [ ] iPad Windows App: solid / gradient test pattern visible.
7. [x] FreeRDP: same, or defer with a named reason in the log below.
8. Desktop Windows App: optional.

Lab process (unblocks 6–7; not a Launch Agent): `just serve-pattern` → `mrdpd-pattern` on `127.0.0.1:3390`. Covered by `tests/pattern_serve.rs`. FreeRDP: `just test-freerdp` (`sdl-freerdp` 3.30.0, dummy SDL, GDI BGRA32).

## Traceability

9. [x] T1-GFX-01 stays `e2e-green`; move toward `interop-green` on C-IPAD if the iPad run lands.
10. [x] T1-GFX-02 at least `e2e-green` if bitmap/RLE is exercised, or document RemoteFX-only with T1-GFX-03 as the path.

## Notes (TDD + ISP)

- FrameKit test went red on `pattern1080p` as 2×2, then green as 1920×1080 quadrants (scaled `bgra2x2`). `FrameSource` still has only `nextFrame()`.
- No new C ABI symbols. EngineKit composition uses existing `push` + poison-after-return with a larger copy buffer.
- `mrdpd-pattern` went red on a stub that never printed `listening`, then green as start + `push_frame` + park. Unspecified bind (`0.0.0.0`) exits 4 (T1-SEC-04).
- T1-GFX-02 remains `untested`: the IronRDP client advertises RemoteFX, so the server does not fall back to RDP 6 / RLE. Forcing RLE would be a new test-only ABI or a second client config — not added.

## Interop log

- Date: 2026-08-20
- Client + version: IronRDP headless 0.17 (`pattern_1080.rs`, `pattern_serve.rs`); FreeRDP **3.30.0** `sdl-freerdp` (`just test-freerdp`)
- Host macOS: darwin 25.3.0
- Result: pass (C-HEADLESS). pass (C-FREERDP): TLS + NLA + GDI `PIXEL_FORMAT_BGRA32` software renderer. **Deferred:** iPad Windows App — no device on this machine; use `just serve-pattern host=<tailscale-ip>`.
- Evidence: quadrant BMP `target/e2e-1080p.bmp` / `target/e2e-pattern-bin.bmp`. FreeRDP log `target/freerdp-client.log` (`gdi_init_ex`: local and remote `PIXEL_FORMAT_BGRA32`; `+rfx`). Wire codec: RemoteFX (T1-GFX-03).
- Follow-up IDs: T1-GFX-02 RLE still untested; C-IPAD pattern still required for full M2 DoD.
