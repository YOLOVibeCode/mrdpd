# Spikes

Drop findings here using the template in [risks.md](../risks.md).

Required in **M1** before claiming M1 done:

- R1 — IronRDP static multimon / GCC monitor data (server)
- R2 — MS-RDPEI touch server
- R5 — reconnect cookies

Until those files exist, M1 is not done.
